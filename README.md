# CargoStockManager Builds

Public build repository for the `CargoStockManager` PrestaShop module.

This repository is intended for:
- signed ZIP releases of the module
- `latest.json` update manifest
- release notes or build metadata when needed
